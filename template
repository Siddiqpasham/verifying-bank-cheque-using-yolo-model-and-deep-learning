<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Cheque Verification System</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <header>
        <h1>Cheque Verification System</h1>
        <nav>
            <a href="{{ url_for('home') }}">Home</a>
            <a href="{{ url_for('addaccountpage') }}">Add Account</a>
            <a href="{{ url_for('viewaccountspage') }}">View Accounts</a>
            <a href="{{ url_for('uploadcheque') }}">Upload Cheque</a>
            <a href="{{ url_for('logout') }}">Logout</a>
        </nav>
    </header>

    {% with messages = get_flashed_messages(with_categories=true) %}
    {% if messages %}
        <div class="flash-messages">
            {% for category, message in messages %}
                <div class="flash {{ category }}">{{ message|safe }}</div>
            {% endfor %}
        </div>
    {% endif %}
    {% endwith %}

    <main>
        {% block content %}{% endblock %}
    </main>

    <footer>
        <p>© 2025 Cheque Verification System</p>
    </footer>
</body>
</html>
{% extends "base.html" %}
{% block content %}
<div class="container">
    <h2>Welcome to Cheque Verification System</h2>
    <p>Use the navigation bar to manage accounts or upload cheques.</p>
</div>
{% endblock %}
{% extends "base.html" %}
{% block content %}
<div class="form-container">
    <h2>Login</h2>
    <form method="post" action="{{ url_for('login') }}">
        <input type="email" name="email" placeholder="Email" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Login</button>
    </form>
</div>
{% endblock %}
{% extends "base.html" %}
{% block content %}
<div class="form-container">
    <h2>Add New Account</h2>
    <form method="post" action="{{ url_for('addaccount') }}" enctype="multipart/form-data">
        <input name="name" placeholder="Full Name" required>
        <input name="email" type="email" placeholder="Email" required>
        <input name="phone" placeholder="Phone Number" required>
        <input name="address" placeholder="Address">
        <input name="accno" placeholder="Account Number" required>
        <input name="amount" type="number" placeholder="Initial Amount" required>
        <label>Upload Signature:</label>
        <input type="file" name="simage" accept="image/*" required>
        <button type="submit">Add Account</button>
    </form>
</div>
{% endblock %}
{% extends "base.html" %}
{% block content %}
<h2>Accounts List</h2>
<table>
    <tr>
        <th>ID</th><th>Name</th><th>Email</th><th>Account No</th><th>Amount</th><th>Action</th>
    </tr>
    {% for r in res %}
    <tr>
        <td>{{ r[0] }}</td>
        <td>{{ r[1] }}</td>
        <td>{{ r[2] }}</td>
        <td>{{ r[5] }}</td>
        <td>{{ r[6] }}</td>
        <td>
            <a class="btn" href="{{ url_for('deposit', id=r[0]) }}">Deposit</a>
            <a class="btn danger" href="{{ url_for('deleteacc', id=r[0]) }}">Delete</a>
        </td>
    </tr>
    {% endfor %}
</table>
{% endblock %}
{% extends "base.html" %}
{% block content %}
<div class="form-container">
    <h2>Upload Cheque</h2>
    <form method="post" action="{{ url_for('upload_cheque') }}" enctype="multipart/form-data">
        <input type="file" name="cimage" accept="image/*" required>
        <button type="submit">Upload</button>
    </form>
</div>
{% endblock %}
{% extends "base.html" %}
{% block content %}
<h2>Deposit Amount</h2>
{% for r in res %}
<form method="post" action="{{ url_for('depositamount') }}">
    <input type="hidden" name="id" value="{{ r[0] }}">
    <input type="hidden" name="email" value="{{ r[2] }}">
    <p>Account Number: <b>{{ r[5] }}</b></p>
    <p>Current Amount: ₹{{ r[6] }}</p>
    <input name="dep" type="number" placeholder="Deposit Amount" required>
    <input type="hidden" name="amount" value="{{ r[6] }}">
    <button type="submit">Deposit</button>
</form>
{% endfor %}
{% endblock %}
body {
    font-family: Arial, sans-serif;
    margin: 0;
    background: #f5f6fa;
    color: #333;
}

header {
    background: #2c3e50;
    color: white;
    padding: 15px 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

header h1 {
    margin: 0;
    font-size: 20px;
}

nav a {
    color: white;
    margin-left: 15px;
    text-decoration: none;
    font-weight: bold;
}

nav a:hover {
    text-decoration: underline;
}

.container {
    padding: 20px;
}

.form-container {
    max-width: 400px;
    margin: 30px auto;
    background: white;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0,0,0,0.1);
}

.form-container input,
.form-container button {
    width: 100%;
    padding: 10px;
    margin-top: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
}

.form-container button {
    background: #2980b9;
    color: white;
    border: none;
    cursor: pointer;
}

.form-container button:hover {
    background: #1f6690;
}

table {
    width: 90%;
    margin: 20px auto;
    border-collapse: collapse;
    background: white;
}

table th, table td {
    padding: 10px;
    border: 1px solid #ddd;
    text-align: center;
}

table th {
    background: #2980b9;
    color: white;
}

.btn {
    padding: 5px 10px;
    background: #27ae60;
    color: white;
    text-decoration: none;
    border-radius: 4px;
}

.btn.danger {
    background: #c0392b;
}

.flash-messages {
    margin: 10px;
    padding: 10px;
    text-align: center;
}

.flash {
    padding: 10px;
    background: #f1c40f;
    color: black;
    margin-bottom: 5px;
    border-radius: 5px;
}
